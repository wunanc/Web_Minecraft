---
title: config.yml
createTime: 2026/08/16 00:00:00
permalink: /giftcode/doc/config-ref/config/
---

```yaml :collapsed-lines
# 插件使用的语言文件（不需要加 .yml 后缀）
# 可选值：zh_cn、en_us
language: 'zh_cn'
```

修改后执行 `/gc reload`。如果填写的语言文件不存在，插件会自动回退到 `zh_cn.yml`。
